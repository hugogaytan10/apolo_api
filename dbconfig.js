// Usar esta configuración para manejar un servidor local de MySQL
const config = {
    host     : 'localhost',
    user     : 'root',
    password : 'root',
    database : 'apoloDB',
}
module.exports = config;